version: 1
applications:
  - frontend:
      phases:
        prebuild:
          commands:
            - npm ci
        build:
        commands:
            - npm run build
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    app-root: app-client